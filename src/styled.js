import styled from "styled-components";

export const AppWrapper = styled.div`
  /* width: 100vw;
  height: 100vh;   */
  /* margin: 0;
  padding: 0;
  display: flex;
  justify-content: center;
  flex-wrap: wrap;  
  box-sizing: border-box;
  background: ${props => props.isOpen && 'red'};   */
`;

